import locale from 'element-plus/dist/locale/es.mjs';
import messageJson from '../json/es.json';

const lang = {
  el: locale.es, // Configuración de i18n de element-plus
  message: messageJson.message, // Configuración de i18n de mensajes personalizados
};

export default lang;
