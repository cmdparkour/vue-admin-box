import locale from 'element-plus/dist/locale/en.mjs'
import messageJson from '../json/en.json'

const lang = {
  el: locale.el, // element-plus i18 setting
  message: messageJson.message,
}

export default lang