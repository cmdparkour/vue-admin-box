import locale from 'element-plus/dist/locale/pt.mjs'
import messageJson from '../json/pt.json'

const lang = {
  el: locale.el, // Configuração de i18n do Element Plus
  message: messageJson.message, // Configuração de i18n de mensagens personalizadas
}

export default lang
